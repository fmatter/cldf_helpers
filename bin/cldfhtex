#!/usr/bin/env python

import cldf_helpers
import sys
import pyperclip

src_string = sys.argv[1]
sources = src_string.split("; ")
tex_string = cldf_helpers.cite_a_bunch(sources, parens=True)
print(tex_string)
pyperclip.copy(tex_string)
